const getSources = (req, res) => {
    res.status(200).send(
        '<p style="color:salmon; margin-top: 5%;">TODO: collection of investing quotes from different books. Incl.: book, author </p>'
    )
}

module.exports = {
    getSources
} 